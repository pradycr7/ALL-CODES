#include <bits/stdc++.h>
using namespace std;
int main()
{

    int T, N;
    cin >> T;

    while (T > 0)
    {
        cin >> N;
        cout << fixed << setprecision(0) << sqrt(N) << endl;

        T--;
    }

    return 0;
}